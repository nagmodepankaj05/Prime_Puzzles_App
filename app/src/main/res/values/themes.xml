<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrimePuzzles" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/pp_background</item>
        <item name="android:navigationBarColor">@color/pp_background</item>
        <item name="android:windowBackground">@color/pp_background</item>
        <item name="android:colorAccent">@color/pp_primary</item>
    </style>

    <style name="Theme.PrimePuzzles" parent="Base.Theme.PrimePuzzles" />
</resources>